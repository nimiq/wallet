import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'wallet.nimiq.com',
  appName: 'Nimiq Wallet',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
